apiVersion: v1
kind: Service
metadata:
  name: aceest-svc
spec:
  type: NodePort
  selector:
    app: aceest
    version: blue   # Initially points to blue
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
